apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: drova-local

resources:
  - 00-namespace.yaml
  - 10-postgres.yaml
  - 20-kafka.yaml
  - 30-trip-service.yaml
  - 40-scaledobject.yaml
# 50-loadgen-job.yaml is applied on demand via `make load` (it is a one-shot Job).
